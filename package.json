{
  "name": "turborepo-basic-shared",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:web": "turbo run build --scope=@outp0st/web",
    "build:payload": "SKIP_PREFLIGHT_CHECK=true DISABLE_ESLINT_PLUGIN=true turbo run build --scope=@outp0st/payload --scope=outpost-payload --include-dependencies --no-deps",
    "dev": "turbo run dev --parallel",
    "dev:o": "turbo run dev --scope=outpost-payload",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "standard-version",
    "test:lib": "turbo run test --scope='@top/core'"
  },
  "devDependencies": {
    "@spotify/prettier-config": "^13.0.0",
    "ava": "^3.15.0",
    "prettier": "^2.5.1",
    "standard-version": "^9.3.2",
    "tsconfig-paths": "^3.12.0",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "npm@8.6.0",
  "dependencies": {
    "@terra-money/terra.js": "^3.0.7",
    "dotenv": "^16.0.0",
    "handlebars": "^4.7.7",
    "minimist": "^1.2.6",
    "nextjs-cors": "^2.1.0",
    "web3.storage": "^4.0.0",
    "winston": "^3.6.0"
  }
}
